<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #4F46E5; color: white; padding: 20px; text-align: center; }
        .content { padding: 20px; background: #f9fafb; }
        .button { display: inline-block; padding: 10px 20px; background: #4F46E5; color: white; text-decoration: none; border-radius: 5px; }
        .footer { text-align: center; padding: 20px; color: #666; font-size: 12px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Paylaşımınız Yayınlandı!</h1>
        </div>
        <div class="content">
            <p>Merhaba <%= user.name %>,</p>
            <p>Instagram paylaşımınız başarıyla yayınlandı.</p>
            
            <h3>Paylaşım Detayları:</h3>
            <ul>
                <li>Başlık: <%= post.title %></li>
                <li>Tarih: <%= new Date(post.published_at).toLocaleString('tr-TR') %></li>
            </ul>

            <p>
                <a href="<%= viewUrl %>" class="button">Instagram'da Gör</a>
            </p>
        </div>
        <div class="footer">
            <p>Bu e-posta Instagram AI Marketing tarafından gönderilmiştir.</p>
        </div>
    </div>
</body>
</html> 